<configuration>
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- call-id: app request id (X-Request-Id). trace_id: OpenTelemetry trace, so logs
                 correlate with traces in Grafana/Tempo. trace_id is blank when no span is active. -->
            <pattern>%d{YYYY-MM-dd HH:mm:ss.SSS} [%thread] %X{call-id} trace_id=%X{trace_id} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    <!-- Copies the active span's trace_id/span_id/trace_flags into MDC before delegating to STDOUT. -->
    <appender name="OTEL" class="io.opentelemetry.instrumentation.logback.mdc.v1_0.OpenTelemetryAppender">
        <appender-ref ref="STDOUT"/>
    </appender>
    <root level="INFO">
        <appender-ref ref="OTEL"/>
    </root>
    <logger name="org.eclipse.jetty" level="INFO"/>
    <logger name="io.netty" level="INFO"/>
</configuration>
